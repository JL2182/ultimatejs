// Expresion ? 'si es true' : 'si es false'

let edad = 16;
let acceso = edad > 17 ? 'Permitir ingreso' : 'No puede ingresar';
console.log(acceso);

let edad2 = 18;
let acceso2 = edad2 > 17 ? 'Permitir ingreso' : 'No puede ingresar';
console.log(acceso2);

// Es muy parecido  a un if, pero más conciso y fácil de leer. Es una forma de simplificar la  estructura de un if. 
